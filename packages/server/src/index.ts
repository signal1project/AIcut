// @mas/server — platform adapters, OAuth service, AI provider abstraction, publish pipeline.
// Ported from the upstream aitoearn backend libs (US platforms only) across tasks #5–15.
// Staging location for ported platform libs: ../../electron/main/plat/libs

export const MAS_SERVER_VERSION = '1.0.0';
